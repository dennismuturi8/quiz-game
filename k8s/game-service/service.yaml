apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: quiz-game
  labels:
    app: game-service
spec:
  selector:
    app: game-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
